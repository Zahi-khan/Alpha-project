import { useQuery } from "@tanstack/react-query"; import { Link,useParams } from "react-router-dom"; import { getTransactions } from "../../app/api/sessions"; import Card from "../../components/ui/Card"; import styles from "./TransactionsPage.module.css";
export default function TransactionsPage(){const {sessionId}=useParams();const {data,error,isLoading}=useQuery({queryKey:["transactions",sessionId],queryFn:()=>getTransactions(sessionId)});if(isLoading)return <main className="container page">Loading transactions…</main>;if(error)return <main className="container page">{error.message}</main>;return <main className={`container page ${styles.page}`}><Link to={`/session/${sessionId}/dashboard`}>← Overview</Link><h1 className="h1">Transactions</h1><Card><div className={styles.table}><div className={styles.row}><b>Date</b><b>Merchant or description</b><b>Category</b><b>Amount</b><b>Confidence</b></div>{data.data.map(item=><div className={styles.row} key={item.id}><span>{item.formatted_date}</span><span>{item.merchant_name??item.fallback_description}</span><span>{item.category_name??"Uncategorized"}</span><strong>{item.formatted_amount}</strong><span>{item.confidence_label}</span></div>)}</div></Card></main>}
