import { useQuery } from "@tanstack/react-query"; import { Link,useParams } from "react-router-dom"; import { getDashboard,reportUrl } from "../../app/api/sessions"; import Card from "../../components/ui/Card"; import InsightCard from "../../components/ui/InsightCard"; import MetricCard from "../../components/ui/MetricCard"; import PrivacyNotice from "../../components/ui/PrivacyNotice"; import StoryNav from "../../components/navigation/StoryNav"; import TypewriterMessage from "../../components/feedback/TypewriterMessage"; import styles from "./DashboardPage.module.css";
export default function DashboardPage(){const {sessionId}=useParams();const {data,error,isLoading}=useQuery({queryKey:["dashboard",sessionId],queryFn:()=>getDashboard(sessionId)});if(isLoading)return <main id="main-content" className="container page"><TypewriterMessage text="Patience is a virtue"/></main>;if(error)return <main id="main-content" className="container page">{error.message}</main>;return <><div className="container"><StoryNav sessionId={sessionId}/></div><main id="main-content" className={`container page ${styles.page}`}><header className={styles.header}><div><p className={styles.eyebrow}>Your financial story</p><h1 className="h1">Your financial snapshot</h1></div><div><span>Private session - expires automatically</span><a href={reportUrl(sessionId)}>Download report</a></div></header><PrivacyNotice/><section id="snapshot" className={styles.metrics}>{data.summary_cards.map(card=><MetricCard key={card.id} card={card}/>)}</section><section id="spending" className={styles.columns}><Card><h2 className="h2">Where your money went</h2>{data.charts[0]?.empty_state?<p>{data.charts[0].empty_state.message}</p>:<div className={styles.chart}>{data.charts[0]?.series?.[0]?.values?.map((point,index)=><div key={point.x} style={{height:`${Math.min(100,Math.abs(Number(point.y))/100)}px`,animationDelay:`${index*80}ms`}} title={`${point.x}: ${point.y}`}/>)}</div>}</Card><Card><h2 className="h2">Data quality</h2><p>SmartSpend has prepared a verified summary for this temporary session.</p></Card></section><section id="patterns"><div className={styles.sectionHeader}><h2 className="h2">Patterns we found</h2><Link to={`/session/${sessionId}/insights`}>View all</Link></div><div className={styles.insights}>{data.top_insights.length?data.top_insights.map(insight=><InsightCard key={insight.id} insight={insight} sessionId={sessionId}/>):<Card>Not enough comparable history is available for reliable patterns yet.</Card>}</div></section><section id="attention" className={styles.storyPanel}><h2 className="h2">What deserves attention</h2><p>Review any uncertain classifications or unusual activity in your transaction explorer.</p><Link to={`/session/${sessionId}/transactions`}>Explore detailed transactions</Link></section><section id="next-steps" className={styles.storyPanel}><h2 className="h2">What you can do next</h2><p>Review your report, revisit supporting evidence, or delete this private session when you are finished.</p><a href={reportUrl(sessionId)}>Download private report</a></section></main></>}
